var express=require('express');var mongoose=require('mongoose');var router=express.Router();
var UserSchema=new mongoose.Schema({name:{type:String,required:true,trim:true},username:{type:String,required:true,unique:true,trim:true,lowercase:true},password:{type:String,required:true}},{timestamps:true});
var User=mongoose.model('User',UserSchema);
router.post('/register',function(req,res){var name=req.body.name;var username=req.body.username;var password=req.body.password;if(!name||!username||!password){return res.status(400).json({success:false,message:'All fields are required'});}if(password.length<4){return res.status(400).json({success:false,message:'Password must be at least 4 characters'});}User.findOne({username:username.toLowerCase()}).then(function(existing){if(existing){return res.status(400).json({success:false,message:'Username already taken!'});}return User.create({name:name,username:username.toLowerCase(),password:password});}).then(function(user){if(user)res.status(201).json({success:true,message:'Account created successfully!'});}).catch(function(err){res.status(500).json({success:false,message:err.message});});});
router.post('/login',function(req,res){var username=req.body.username;var password=req.body.password;if(!username||!password){return res.status(400).json({success:false,message:'All fields are required'});}User.findOne({username:username.toLowerCase()}).then(function(user){if(!user){return res.status(400).json({success:false,message:'Username not found!'});}if(user.password!==password){return res.status(400).json({success:false,message:'Wrong password!'});}res.json({success:true,message:'Login successful!',token:'token_'+user._id,user:{name:user.name,username:user.username}});}).catch(function(err){res.status(500).json({success:false,message:err.message});});});
module.exports=router;